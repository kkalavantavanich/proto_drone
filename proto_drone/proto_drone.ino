/*
 * This code works only with MultiWii Crius v2.5
 * - AT Mega 328p board
 * - sensors built-in board
 *
 * =========== DIRECTORY ============
 * - `main` code is in [proto_drone.cpp] -- setup() and loop()
 *
 * - sensors code is in [I2C.cpp] -- sensors with I2C interface
 * --- External Pins (ext)
 * --- EEPROM (eeprom)
 * --- Gyrometer and Accelerometer (ga)
 * --- Barometer (baro)
 * --- Compass (cmps)
 *
 * - `print()` code is in [USART.cpp]
 */
